<%= form_for(signup) do |f| %>
  <% if signup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(signup.errors.count, "error") %> prohibited this signup from being saved:</h2>

      <ul>
      <% signup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= stylesheet_link_tag "Format_New_Support" %>

  <div id = "form">

    <div class="field">
      <%= f.label :Name %>
      <%= f.text_field :Name %>
    </div>

    <div class="field">
      <h3></h3>
      <%= f.label :Email %>
      <%= f.text_field :Email %>
    </div>

    <div class="field">
      <h3></h3>
      <%= f.label :Username %>
      <%= f.text_field :Username %>
    </div>

    <div class="field">
      <h3></h3>
      <%= f.label :Password %>
      <%= f.text_field :Password %>
    </div>

    <div class="actions">
    <h3></h3>
      <%= f.submit "Submit" %>
    </div>

  </div>
<% end %>
